# Package contents are controlled by the `files` allowlist in package.json.
# This file is only belt-and-suspenders for anything that slips inside an
# allowlisted directory.

# tarballs generated by `npm pack`
/*.tgz

# tests & mocks
__mocks__
__tests__

# TypeScript incremental build cache
*.tsbuildinfo
